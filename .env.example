# aicentral v4.0+ 設定已改為 YAML，不再讀取本檔的 API 金鑰與預設模型。
#
# 1. 複製機密範本並填入真實值：
#      Copy-Item config\secret.yaml.example config\secret.yaml   (PowerShell)
#      cp config/secret.yaml.example config/secret.yaml         (Unix)
#
# 2. 調整主設定（可提交版控）：
#      config/aicentral.yaml
#
# secret.yaml 使用巢狀結構（ollama / openai / anthropic / gemini / mcp）。
# aicentral.yaml 以 secret/ollama.api_key 等形式引用，由 loader 展開。
#
# 未傳 model 時的預設：defaults.model（別名，如 local-chat）→ secret.ollama.model → ollama/{model}
#
# 舊版 .env 變數（AICENTRAL_DEFAULT_MODEL、OLLAMA_*、OPENAI_API_KEY 等）已廢止。
