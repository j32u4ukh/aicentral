---
description: aicentral Python 撰寫規範
globs: "**/*.py"
alwaysApply: false
---

# Python 撰寫規範

- 註解與區塊說明：見 [code-comments.mdc](./code-comments.mdc)。
- 目標版本 Python 3.11+；公開函式與方法需有型別標註。
- 路徑優先使用 `pathlib.Path`，避免字串路徑拼接。
- 機密由 `config/secret.yaml` 經 `get_secret()` 載入，不得在原始碼硬編碼 API 金鑰。
- 結構化輸出使用 Pydantic 模型（Instructor 風格，但不依賴 `instructor` 套件）。
- LLM 呼叫經 `complete()` / `routing` / `providers`，勿在消費方繞過直接打各供應商 HTTP。
- 模組職責單一；一次性腳本放 `scripts/`，勿塞進 `src/aicentral/`。

```python
from aicentral.config import get_config, get_secret

cfg = get_config()
api_key = get_secret("openai.api_key")
```
